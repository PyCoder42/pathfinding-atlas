# Dependencies (the app itself is dependency-free; jsdom is only for tests)
node_modules/
package-lock.json

# OS / editor
.DS_Store
*.swp
.vscode/

# Logs / scratch
*.log
/tmp/

# Raw Overpass dumps (regenerable via tools/refetch-osm.sh; only baked data/ is committed)
tools/osm-raw/
